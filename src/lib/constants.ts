export const API_URL = (process.env.NEXT_PUBLIC_API_URL || "http://localhost:8080").trim();
export const FRONTEND_URL = (process.env.NEXT_PUBLIC_FRONTEND_URL || "http://localhost:5173").trim();
export const LANDING_URL = (process.env.NEXT_PUBLIC_LANDING_URL || "http://localhost:3000").trim();
